# regex in json schema works

    Code
      metadata(file)
    Condition
      Error:
      x The YAML input has an incorrect structure.
      x Option `id.var` must contain a valid variable name.
      i Look at `?make.names` for details.

---

    Code
      metadata(file)
    Condition
      Error:
      x The YAML input has an incorrect structure.
      x Option `id.var` must contain a valid variable name.
      i Look at `?make.names` for details.

---

    Code
      metadata(file)
    Condition
      Error:
      x The YAML input has an incorrect structure.
      x Option `id.var` must contain a valid variable name.
      i Look at `?make.names` for details.

